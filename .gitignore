build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
test/run
*.o

# Never commit credentials.  See tools/check_no_secrets.sh.
credentials.conf
*.local.conf
sdkconfig.defaults.local

# Overrides PROJECT_VER for local experiments (installing an older build to
# test an update, say).  Committing it would stamp a release with whatever it
# happens to contain, so it never belongs in the repository.
version.txt
