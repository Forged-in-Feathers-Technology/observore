# Observore partition table.
#
# OTA-shaped, deliberately, even though OTA is not implemented. The layout is
# the part that is expensive to change later: the browser flasher writes the
# partition table, so a device upgrading to a different layout would silently
# lose whatever moved. Fixing the offsets now, while there are few devices,
# costs nothing and means it never has to happen.
#
# Two offsets are the actual commitment, and only two:
#
#   nvs at 0x9000   -- every stored setting. Mute rules, Wi-Fi credentials, the
#                      notifier token, the generated console password and the
#                      detection history all live here. It has never moved and
#                      must never move; it is where ESP-IDF's
#                      partitions_singleapp_large.csv put it, which is what the
#                      earliest firmware used.
#   ota_0 at 0x20000 -- the running application.
#
# Everything else can still change. ota_1 holds no state, so its size and
# position are free to move later, and even growing ota_0 is non-destructive:
# the running app stays where it is and only the scratch slot shifts.
#
# Sized for a 4 MB board, which is the smallest supported (the C6-DevKitM-1).
# The reference C5 and C6 DevKitC-1 kits carry 8 MB and Waveshare's C5 kit 16
# MB; those simply leave the remainder unused, because an image built for more
# flash than the chip has does not boot at all while the reverse is harmless.
#
# Slot occupancy at the time of writing: C5 83%, C6 78%, S3 64%. The C5 is the
# tightest and the fastest growing, so an overflow is plausible -- but CI builds
# every target, so it fails there rather than in the field, exactly as it did
# when the old 1500K partition overflowed by 27 KB.
#
# Name,     Type, SubType, Offset,    Size,  Flags
nvs,        data, nvs,     0x9000,    24K,
phy_init,   data, phy,     0xf000,    4K,
otadata,    data, ota,     0x10000,   8K,
ota_0,      app,  ota_0,   0x20000,   1984K,
ota_1,      app,  ota_1,   0x210000,  1984K,
